#include <stdio.h>
#include <stdlib.h>
#include "optr.h"

int main()
{
/*
	struct optr_node *root = optr_alloc(S_ADD, T_ADD, 1);
	struct optr_node *a = optr_alloc(S_ZERO, T_VAR, 0);
	struct optr_node *b = optr_alloc(S_ONE, T_VAR, 0);
	struct optr_node *times = optr_alloc(S_TIMES, T_TIMES, 1);
	struct optr_node *c = optr_alloc(S_PI, T_VAR, 0);
	struct optr_node *d = optr_alloc(S_DOTS, T_VAR, 1);
	struct optr_node *add = optr_alloc(S_FUN_CLASS, T_ADD, 1);
	struct optr_node *e = optr_alloc(S_PI, T_VAR, 0);
	struct optr_node *f = optr_alloc(S_DOTS, T_VAR, 0);
	struct optr_node *sqrt = optr_alloc(S_NEG, T_NEG, 0);
	struct optr_node *g = optr_alloc(S_DOTS, T_VAR, 1);

	optr_attach(a, root);
	optr_attach(b, root);

	optr_attach(c, times);
	optr_attach(d, times);
	optr_attach(times, root);

	optr_attach(e, add);
	optr_attach(f, add);
	optr_attach(add, root);

	optr_print(root, stdout);
	optr_release(root);
*/

	return 0;
}
