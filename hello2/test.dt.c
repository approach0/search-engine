#header "a.h" {
	#require "dat.h"
	int foo(struct dat);
	
	#require "tad.h"
	int bar(struct tad);
}

#require "list.h"

sam#["age"]
sam # [ "name" ]
sam#["office hour"]

foreach (iter1, list,li) {
	if (abc) {
		efz;
	} else if (z) {
		foreach (iter2, list,li) {
			bar;
		}
	}
	do_something();
}

helloworld(i++)

