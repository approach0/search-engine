#require "dat.h"
int foo(struct dat);
	
#require "tad.h"
int bar(struct tad);

#require "dta.h"

idx_arr[127]
idx_arr[a++]
idx_arr[a + 1]

sean_sam["age"]
sean_sam [ "name" ]
sean_sam["office hour"]

foreach (iter1, list,li) {
	if (fun) {
		a++;
		a += 1
	} else if (sad) {
		foreach (iter2, list,li) {
			foo;
			bar;
		}
	}
	do_something();
}

goodbye()
